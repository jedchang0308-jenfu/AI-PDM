# DEV-109 CAPA Local Verification Receipt

日期：2026-09-01  
CAPA：`CAPA-DEV-109-SLDASM-CANDIDATE-001`  
執行範圍：task-owned SQLite、disposable PostgreSQL、歷史 DEV-109 unified regression；`productionWrites=false`、`productionConnection=false`。

## 結果

| Gate | 結果 | 證據 |
|---|---|---|
| CAPA-L01 | PASS | preparation current state、active primary exact `.SLDASM` promotion + audit |
| CAPA-L02 | PASS | drawing RD/preparing、production/released、replay already-assembly no-op |
| CAPA-L03 | PASS | SLDPRT、removed、reference、non-current、ambiguous、cross-company、terminal zero-write |
| CAPA-L04 | PASS | candidate GET `assembly_file`、`classify`、zero-write |
| CAPA-L05 | PASS | A0044-shaped pre/post reconcile：`classify → create`、bounded deterministic ordering |
| CAPA-L06 | PASS | reconcile dry-run/apply/rerun：2 targets、2 applied、rerun 0；scope fingerprint／plan hash |
| CAPA-L07 | PASS | SQLite candidate readback + disposable PostgreSQL provider gate + CAPA-PG-01 exact SLDASM promotion/replay |
| CAPA-L08 | PASS | historical unified aggregate observed 54/54、missing=[] |

Evidence：`output/qa/dev-109-capa/2026-09-01T02-31-21-606Z/capa.json`；歷史回歸 evidence：同目錄 `historical-54/aggregate.json`。  
工程 gate：`npm.cmd run typecheck:app` PASS、受影響檔案 ESLint PASS、`npm.cmd run qc:dev-109:unified:contract` PASS、`npm.cmd run qc:dev-109:unified:repository` PASS；CAPA runner 完成後 task root、SQLite handle、child PostgreSQL／Chromium runtime 均清理。

2026-09-01 continuation fresh unified aggregate：`npm.cmd run qc:dev-109:unified` 完成 `54/54 PASS`（C01-C08、M01-M08、P01-P06、R01-R14、T01-T08、B01-B10，`missing=[]`），provider／browser 均為 task-owned、`productionWrites=false`、`productionConnection=false`；evidence=`output/qa/dev-109-unified/2026-09-01T05-44-34-610Z/aggregate.json`。Provider receipt 確認 disposable PostgreSQL `portReleased=true`、`clusterStopped=true`、`taskRootRemoved=true`；Chromium 動態 port `56876` 結束後未留 listener。此為 current local regression evidence，不取代 production P01～P04。

同一 working-tree snapshot 的 isolated production build 亦通過：`npm.cmd run build:isolated` exit `0`、Next artifact `BUILD_ID` 存在、primary SQLite logical invariant hash before/after=`45897e3fae0ebc5aa851d8e4036de7a1c6f173441026d830fb787bf2a230153f`、task-owned runtime cleanup=`true`。此 build receipt 仍不具 exact release commit provenance。

Source scope overlap receipt（2026-09-01 05:54:47Z）顯示 DEV-032 plan 的 298 個 included paths 中，只有 52 個與 DEV-109 §31.10 的 54-file planning ledger 交集，另有 246 個非 CAPA paths；`src/lib/bom-submission-diff.ts` 與 `src/lib/repositories/numbering-repository.ts` 不在目前 included list。證據=`output/qa/dev-109-capa-production/2026-09-01T0315Z/source-scope-overlap.json`；因此禁止直接 stage 整份 298-path plan。

Release-owner decision packet=`output/qa/dev-109-capa-production/2026-09-01T0315Z/release-owner-decision-packet.json`，列出 CAPA-only hunk re-baseline 與 mixed 298-path scope 的決策點，以及後續 exact commit／artifact／P03／P04 gate 順序。

## 修正內容

- Drawing-level SLDASM reconcile 改以 current canonical revision＋active primary exact `.SLDASM`＋same-company formal unique `primary_manufacturing` relation判定；不再以 data layer、revision lifecycle 或 work binding作必要條件。
- Candidate read projection 同一語意邊界，對外 reason 統一為 `assembly_file`／「有組合檔」，action 仍只由 Part `structure_type`、權限、生命週期與既有 BOM 決定；GET 不寫資料。
- Existing-data reconcile runner 增加 scope fingerprint、plan hash、terminal zero-write、audit before/after，apply 只限 exact plan rows，rerun 必須 no-op。
- 新增 task-owned `scripts/qc-dev-109-capa.mjs` 與 `qc:dev-109:capa`，固定保留 CAPA-L01～L08，不改寫歷史 54 案 manifest。

## 正式環境邊界

本 receipt 只代表 local CAPA corrected，不代表 production corrected 或 CAPA closed。CAPA-P01～P04 仍須由 DEV-032 Release Capsule 以正式資料 read-only inventory、明確核准的 reconcile apply、同一 immutable artifact feature smoke，以及一次 scheduled read-only effectiveness audit 完成；本 receipt 不授權正式資料、activation、deploy 或 release。

DEV-032 preflight（2026-09-01）先執行不連線的 run-plan dry-run：`node scripts/run-dev-032-production-reconciliation.mjs`，`connectionAttempted=false`、expected migrations=`49`（當時 release package 的基線，不代表正式庫實際 migration max）、readback SQL SHA-256=`97fbf0eef133a385d626d8084f68f6de040876819ef73382d1c17fea13c238ad`。使用者授權續接後，已透過正式 Cloud Run migration runner 的一次性 read-only execution 完成 CAPA-P01 inventory；機器收據：`output/qa/dev-109-capa-production/2026-09-01T0315Z/p01-readonly.json`。

## CAPA-P01 正式 read-only 結果（2026-09-01）

| 項目 | 結果 | 正式證據／判定 |
|---|---|---|
| Target／IAM／proxy boundary | PASS | `ai-pdm-prod-migration-runner-cf859`、`-gsxqx`、`-v7fg8`；private VPC、Cloud SQL proxy、IAM migration user；`productionConnection=true`、`productionWrites=false` |
| Backup reference | PASS | latest successful automatic backup `1788199200000`；PITR 與 deletion protection 讀回為啟用 |
| 054 schema inventory | BLOCKED | 正式 `pdm_schema_migrations` 目前只有 `049`～`052`；`053`、`054` 與 unified BOM 欄位／constraints 尚不存在 |
| SLDASM reconcile dry-run | PASS（NO TARGET） | `activePrimarySldasmEvidence=0`、`exactTargetCount=0`、`ambiguous=0`、`crossCompany=0`、`planHash=scopeFingerprint=4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`；禁止 seed |
| A0044 production trace | PASS（事實讀回） | `A0044-P01=Draft/single_part`；`A0044-M01=Draft/drawing_preparation`、revision=`preparing`、data layer=`drawing_rd`；formal relation存在但 active primary `.SLDASM` file rows=`0` |

CAPA-P02 read-only NO-OP（2026-09-01）：execution=`ai-pdm-prod-migration-runner-f78v8`，receipt=`output/qa/dev-109-capa-production/2026-09-01T0315Z/p02-no-op.json`。同一 scope／plan hash 重算仍為 `exactTargetCount=0`；`applied=0`、`auditCountDelta=0`、`rerunNoOp=true`、`postApplyDrift=0`，A0044 before／after unchanged，`productionWrites=false`。這是「沒有 eligible target 所以不寫入」的有效 NO-OP，不等於 054 schema 或 CAPA production corrected 已完成。

CAPA-P01 不得標為完整 PASS：read-only inventory 已完成，但 054 schema 尚未進正式庫，且正式資料沒有可回補的 active primary exact `.SLDASM` target。依契約，CAPA-P02 只保留 `NO_OP_ELIGIBLE_NOT_APPLIED`（exact target=0），不建立任何正式 mutation；CAPA-P03 因缺少本次 CAPA 的 immutable artifact／inactive candidate 且沒有 production-equivalent active SLDASM row，尚未執行；CAPA-P04 必須等 P03 activation 後才可排程。

正式 Cloud Run job template 在上述 execution override 後仍維持原始 migration runner image／args；override 只建立 execution metadata，沒有修改 job template。不得將這些 read-only execution 視為 migration 054 apply、deploy、activation 或 CAPA closure。

## DEV-032 source boundary follow-up（2026-09-01）

為避免把 dirty worktree 誤當成 production artifact，已修正 `contracts/` 與 Next.js generated type reference 的 source 分類；source manifest QC=`14/14 PASS`、commit plan QC=`12/12 PASS`。最新雙快照 stability receipt（2026-09-01 05:04Z）在產生當下為 `stable=true`、`includedProductionSourceEntries=269`、`unknownRisk=0`，但工作區仍 dirty、尚未建立 exact release commit，因此 `safeToBuildForProduction=false`。該 receipt 是 point-in-time gate；任何後續 source 變更都會使其失效，必須重新產生雙快照。證據：`output/qa/dev-109-capa-production/2026-09-01T0315Z/source-boundary-stability.json`；commit plan 僅作 source-set／pathspec 參考，不代表已 stage／commit。後續仍未 stage／commit／push，亦未改變正式資料。

本次依 SPEC §31.10 的 54-file planning ledger 做現況重算：僅 `14/54` bytes 與 planning hash 相符，`40/54` 已 drift；因此原 ledger 不能直接用來建立 CAPA release commit，必須由 DEV-032 release owner 重新確認 scope／hunk ownership 後再產生新 ledger。機器收據：`output/qa/dev-109-capa-production/2026-09-01T0315Z/source-ledger-current.json`。這是 source-boundary blocker，不是 production data blocker 的替代證據。

最新 DEV-032 commit plan（2026-09-01 05:34Z）觀測雙快照不一致：`stable=false`、`includedProductionSourceEntries=294`、`unknownRiskEntries=0`、`safeToStageIncludedSource=false`、`safeToBuildForProduction=false`；commit-plan QC 已通過 `12/12`。由於本段文件後續更新會再次使 snapshot 失效，該數值只作本次重驗證紀錄，下一次 release-owner review 前仍須重產 plan。

Release-source recheck（2026-09-01 06:16:04Z）重新產生雙快照為 `stable=true`、`includedProductionSourceEntries=298`、`unknownRiskEntries=0`、`safeToStageIncludedSource=true`，但 `safeToBuildForProduction=false`、`exactReleaseCommitExists=false`，blocker=`RELEASE_OWNER_SELECTION_AND_EXACT_COMMIT_STILL_REQUIRED`；commit-plan QC=`12/12`。與 DEV-109 §31.10 planning ledger 交集仍為 `52/54`，另有 `246` 個非 CAPA paths，缺少 `src/lib/bom-submission-diff.ts` 與 `src/lib/repositories/numbering-repository.ts`；machine receipts=`output/qa/dev-109-capa-production/2026-09-01T0315Z/source-scope-overlap-2026-09-01T0616Z.json`、`release-owner-decision-packet-2026-09-01T0616Z.json`。本段更新本身屬 included documentation source，因此該 plan 僅作 point-in-time boundary evidence；在 release owner 選定 scope/hunk 後，必須再產生穩定雙快照與 exact commit，未完成前不得 stage、build、push、deploy、migration apply、activation 或關閉 CAPA。

最新 recheck（2026-09-01 06:21:28Z）在雙快照期間觀測 dirty entries `79,727 → 79,729`，因此 `stable=false`、`includedProductionSourceEntries=298`、`unknownRiskEntries=0`、`safeToStageIncludedSource=false`、`safeToBuildForProduction=false`，blocker=`RELEASE_SOURCE_SNAPSHOT_UNSTABLE`；commit-plan QC=`12/12`。此為並行 CUA/runtime 造成的 source boundary 未凍結，並非 production mutation；在 runtime 停止且 release owner 選定 scope/hunk 前，禁止 stage、build、push、deploy、migration apply、activation 或 CAPA closure。

本輪 root-cause correction：DEV-032 source-plan stability 現改以 `sourceSnapshotSha256`、`classificationSha256`、included source count 與 unknown-risk count 判定；`totalDirtyEntries` 僅保留 telemetry，不再因 generated evidence-only churn 阻擋 source boundary。這不放寬 source/config 變更 gate，也不等於可 stage；修正後必須重新產生 plan、由 release owner 選定 CAPA scope/hunk，並建立 exact commit，才可進入 immutable artifact 與後續 P03/P04。

修正後 recheck（2026-09-01 06:34:06Z）已在 dirty evidence churn 下得到 `stable=true`、included source=`299`、unknown risk=`0`、`safeToStageIncludedSource=true`；仍為 `safeToBuildForProduction=false`、`exactReleaseCommitExists=false`，blocker=`RELEASE_OWNER_SELECTION_AND_EXACT_COMMIT_STILL_REQUIRED`，commit-plan QC=`12/12`。與 §31.10 planning ledger 交集為 `52/54`、非 CAPA paths=`247`；scope／owner selection 與 exact commit 仍是必要 gate。machine receipts=`output/qa/dev-109-capa-production/2026-09-01T0315Z/source-scope-overlap-2026-09-01T0634Z.json`、`release-owner-decision-packet-2026-09-01T0634Z.json`。

Local CAPA static gate recheck（2026-09-01 continuation）：`npm.cmd run typecheck:app` exit `0`；依 SPEC §31.10 抽取的 50 個 TypeScript／JavaScript runner 檔以 `npx eslint --max-warnings=0` exit `0`，`0 errors / 0 warnings`。此結果只證明目前 working-tree snapshot 的靜態品質，不替代 exact source、immutable artifact 或 production smoke gate。

## DEV-109 CAPA continuation（2026-09-02）

依前一輪 scope decision，`DEV-109 CAPA-only` exact commit 為 `6b6d828f9d7e0181b3afb07bffaac75bce1c8ea6`。在 detached clean worktree 以 task-owned data/repository 目錄執行 webpack production build，結果為 `FAIL`：`canonical-change-workspace.tsx` 依賴的三個 shared dependency 尚未進入該 commit：

- `src/components/canonical-change-workspace.module.css`
- `src/components/part-maintenance-workspace-sections.tsx`
- `src/components/use-part-number-matrix-controller.ts`

三個檔案目前存在於主工作樹但均不在 `HEAD` 或 `HEAD^`，且屬其他 DEV 的未提交變更；未經 release-owner scope decision，不得併入 DEV-109 CAPA-only。該 task-owned worktree、build output 與資料目錄已清理，沒有正式資料寫入。

同日 `DEV-032` production target preflight 維持唯讀且 `productionActionPerformed=false`，receipt=`output/dev-032-production-target-preflight/report.json`：gcloud re-authentication failed，production project／Cloud Run／Cloud SQL／Secret metadata 均未能重新讀取；`production-target.template.json` 仍為 `releaseReady=false`、`productionActionAllowed=false`。因此 immutable artifact、P02 apply、P03 activation 與 P04 effectiveness audit 仍不可執行，CAPA 維持 `Production Effectiveness Blocked`。

同日再次以主工作樹執行 `npm.cmd run build:isolated`（task-owned runtime、`PDM_DB_PROVIDER=sqlite`）作為整合源對照：compile 後在 TypeScript gate 失敗，錯誤位於其他 DEV 的 `src/lib/numbering-permission-async.ts`、`src/lib/repositories/jenfu-entitlement-repository.ts` 及測試相依 `vitest`；未產生 BUILD_ID。主 SQLite logical invariant hash 前後均為 `45897e3fae0ebc5aa851d8e4036de7a1c6f173441026d830fb787bf2a230153f`，`primary=true`、`cleanup=true`。此再次證實目前 working-tree 也不能作為 immutable production artifact，且不應以 unrelated TypeScript 修補冒充 DEV-109 CAPA 修正。

CAPA commit 基線分離驗證：父版本 `91de270c3a644dfbcbee49ed255b3c18e13df9dd` 的 clean worktree `typecheck:app` 通過；`6b6d828f9d7e0181b3afb07bffaac75bce1c8ea6` 的 clean worktree 則出現 CAPA contract dependency closure 錯誤：`part-maintenance-workspace-sections`、`use-part-number-matrix-controller`、`preview-derivatives` export 缺口，以及 `PartChangePayload.baseUomCode`、`hasPrimaryManufacturingDrawing`、BOM quantity diff 型別未同步。這些錯誤需由 DEV-109 release integration 補齊，不能以父版本或目前 dirty worktree 的 PASS 代替；兩個 audit worktree 均已清理。

同一 exact commit 的 clean worktree contract gate 隨後完成 `npm.cmd run qc:dev-109:unified:contract`：`C01–C08=8/8 PASS`、`productionWrites=false`。這證明 CAPA 核心 contract 可獨立驗證，但不改變其 application typecheck／immutable artifact dependency closure 未完成的判定。

Dependency-closure audit（2026-09-02）：在不改動主分支的暫存 worktree 補入 exact commit 明確引用、但未進 commit 的 9 個 shared component/library 後，typecheck 錯誤縮小為 4 個 integration sync points：relation matrix 的 `dirtyKeys` prop、canonical workbench 的 `hasPrimaryManufacturingDrawing` projection、review payload 的 `baseUomCode`、以及 BOM repository 的 quantity diff 型別。這些 consumer／shared UI 檔案分屬其他 DEV 的工作樹變更；本 audit 僅用來界定 release integration，不將其視為 CAPA-only 已完成，也未寫回主分支。

Production release-source gate（2026-09-02）：使用者確認 gcloud 已登入後，production target preflight read-only 讀回 project=`jenfu-ai-pdm-prod`、Cloud Run service、Cloud SQL instance=`ai-pdm-prod-postgres` 與 required Secret metadata 全部可讀；以 exact commit 建立 clean manifest，`exactReleaseCommitExists=true`、`releaseCommitSha=6b6d828f9d7e0181b3afb07bffaac75bce1c8ea6`、`unknownRiskEntries=0`、`safeToBuildForProduction=false`。`qc:dev-032-release-source-manifest` 完成 `13/13 PASS`；preflight=`blockerCount=0` 但狀態仍為 `preflight_readonly_passed_release_still_requires_approval`。證據：`output/dev-032-release-source/manifest.json`、`output/dev-032-production-target-preflight/report.json`。此 gate 只解除 source selection／target readability，不代表 artifact build、migration、deploy、activation 或 CAPA closure。

Migration contract recheck（2026-09-02 continuation）：在 exact CAPA commit `6b6d828f9d7e0181b3afb07bffaac75bce1c8ea6` 上以 detached clean worktree 執行 `node scripts/qc-dev-109-unified-migration.mjs`，`M01–M08=8/8 PASS`；確認 SQLite transaction／foreign-key check、全 UOM tables、union-preserving issue constraint、PostgreSQL idempotent lock／purpose retirement、fresh-schema UOM 與 primary-path guard。該 worktree 與 task-owned 目錄已清理；`productionWrites=false`。

Migration scope note：工作區另有未追蹤 `db/postgres/053_drawing_recognition_amendment_lineage.sql`，其來源與 DEV-107 對應，並非 DEV-109 CAPA-only exact commit 的 hunk。DEV-109 054 provider gate 在 S0／S1 disposable PostgreSQL 中已獨立通過；正式庫現況 `053/054` 均缺少時，release owner 必須先在 DEV-032 migration capsule 明確排定 053 的既有 DEV-107 相依性，再核准 054 套用，不得把未追蹤檔案直接混入 CAPA artifact，也不得跳過版本／checksum readback。

CAPA local effectiveness recheck（2026-09-02 continuation）：`node --experimental-transform-types --experimental-loader ./scripts/qc-ts-path-loader.mjs scripts/qc-dev-109-capa.mjs` 完成 `CAPA-L01～L08=8/8 PASS`，包含 candidate reason/action zero-write、preparation／RD／released SLDASM promotion、7 類 negative fail-closed、bounded reconcile `2→2→0`、SQLite／disposable PostgreSQL semantic parity 與歷史 DEV-109 `54/54` regression；`foreignKeyViolations=[]`、`productionWrites=false`、`productionConnection=false`、`cleanup.taskRootRemoved=true`。最新證據：`output/qa/dev-109-capa/2026-09-02T-capa-continuation-2/capa.json`。

Production disposition after re-authentication：登入只解除 target discovery 的認證阻擋；正式 target contract 仍為 `templateOnly=true`、`releaseReady=false`、`productionActionAllowed=false`，release-source manifest 仍為 `safeToBuildForProduction=false`（`PRODUCTION_TARGET_ENV_RESTORE_ROLLBACK_AND_SMOKE_MISSING`）。正式庫 P01 既有 read-only inventory 仍顯示 migration max=`052`、`053/054` 未套用且 active primary exact `.SLDASM` target=`0`；因此不得以本輪 local PASS 或 re-authentication 視為 production migration／reconcile／deploy／activation 完成，CAPA 仍維持 `Production Effectiveness Required / Release Gated`。

DEV-032 preflight QC recheck（2026-09-02）：`node scripts/qc-dev-032-production-target-preflight.mjs` 完成 `16/16 PASS`，確認 report schema、read-only／no-action boundary、production project／provider／env／secret metadata、Cloud Run／Cloud SQL discoverability、exact release manifest reference、template fail-closed contract、command allowlist與secret redaction；此為 gate integrity evidence，不是 production release approval。

DEV-032 activation readiness recheck（2026-09-02）：唯讀產生 `output/dev-032-production-activation-readiness/report.json`，machine-verifiable A0～A7=`8/8 PASS`，總體 `8/10`；A8 仍要求與本次 artifact／serving revision／image digest一致的 authenticated Level 4，A9 仍待 3–5 名明確 Wave 0 使用者與 product-owner go/no-go。此為 DEV-032 generic baseline 的現況，不得把歷史 release 的 A0～A7 或舊 Level 4 證據移植為 DEV-109 CAPA-P03。

Generic live-readback reattempt（2026-09-02）：gcloud 已可登入，但 `capture-dev-032-production-live-readback.mjs` 對既有 execution `ai-pdm-prod-migration-runner-95bn8` 找不到可解析的 JSON runner log，故未產生新的 live-readback receipt；命令未執行任何 mutation，task-owned temporary data／repository 已清理。既有 DEV-109 CAPA-P01/P02 receipts 仍為正式 CAPA 判定依據。

## DEV-109 release integration dependency closure（2026-09-02 continuation）

依 release-owner 已核准的「都納入」範圍，將 exact CAPA commit 的 application dependency closure 與四個 integration sync points 一併納入新的整合提交；範圍固定為 23 個已核對檔案，不包含 299-path mixed dirty worktree、未追蹤 DEV-107 migration `053`、`package.json` 的非 CAPA scripts 或其他未列明路徑。納入項目涵蓋 canonical workbench／三模式 preview、part maintenance／structure／recognition transfer、relation matrix dirty contract、preview derivative projection、review `baseUomCode` 與 BOM quantity diff/UOM contract，以及 drawing recognition async repository。

在 clean detached task-owned worktree 以相同 23-file closure 驗證：

- `npm.cmd run typecheck:app`：PASS。
- `npm.cmd run build:isolated`：PASS；Next `BUILD_ID` 存在，primary SQLite logical invariant 為 database-absent before/after（主工作樹資料庫未被接觸），runtime／artifact cleanup=`true`。
- `qc-dev-109-unified-contract`：C01～C08=`8/8 PASS`；`qc-dev-109-unified-migration`：M01～M08=`8/8 PASS`；`qc-part-recognition-transfer`=`PASS`；均為 `productionWrites=false`。
- DEV-112 three-view browser integration（task-owned fixture，laptop/tablet/mobile）：TVM-001～TVM-024=`24/24 PASS`，包含三模式切換、URL／localStorage、current-page media bound、drawer focus、protected file-read 401/404 probes、primary／fixture invariant、port／process／fixture cleanup；`productionWrites=false`、`productionConnection=false`。

舊 DEV-065 browser script 以現有 A0005 自訂預覽資料執行時，固定 RD-ready-source assertion 會失敗；該結果是既有 fixture state mismatch，不作為本次 closure 的失敗判定，並已由 DEV-112 task-owned fixture 完成等價三模式／安全邊界驗證。上述 23-file closure 已由新的 integration commit 固定 provenance；commit 後須重新產生 DEV-032 release-source manifest，舊 manifest 不得沿用。

本節只解除 local application dependency closure，不代表 production artifact、054／既有 053 migration、正式 reconcile、deploy、activation 或 CAPA closure。正式 target contract 仍維持 `templateOnly=true`、`releaseReady=false`、`productionActionAllowed=false`；CAPA-P03／P04 仍受 DEV-032 immutable artifact、authenticated Level 4 smoke、Wave 0 owner go/no-go 與 scheduled effectiveness audit gate 約束。
